// 开发者工具里可以用 http://127.0.0.1:8000。
// 真机预览时，把这里改成电脑在同一 WiFi 下的局域网 IP，例如 http://192.168.1.23:8000。
// 正式发布时必须换成 HTTPS 域名，并在微信公众平台配置 request/uploadFile 合法域名。
const BASE_URL = 'http://127.0.0.1:8000'

module.exports = {
  BASE_URL
}
